import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#B6F500",
      dark: "#648900ff",
    },
    secondary: {
      main: "#ff1c14ff",
      dark: "#890000ff",
    },
  },
});

export default theme;
